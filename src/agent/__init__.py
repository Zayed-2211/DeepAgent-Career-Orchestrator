"""
Phase 6 — LangGraph Agent Core package.

The agent orchestrates the full career pipeline:
  Intake → Analysis → Matching → Planning → Review → Dispatch

Entry point: src/agent/graph.py (compiled_graph)
CLI:         scripts/run_agent.py
"""
